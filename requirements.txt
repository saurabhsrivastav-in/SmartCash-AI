# --- Core UI & Data ---
streamlit>=1.31.0
pandas>=2.1.0
numpy>=1.24.0
plotly>=5.18.0

# --- Matching Logic Dependencies ---
# Using RapidFuzz instead of fuzzywuzzy because it doesn't require a C++ compiler to install
rapidfuzz>=3.0.0
scikit-learn>=1.3.0

# --- Python Utilities ---
python-dateutil>=2.8.2
