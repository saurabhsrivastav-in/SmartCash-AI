# ==========================================
# SmartCash AI: Enterprise Security Guard
# ==========================================

# --- SYSTEM & OS ---
# Prevent metadata leakage across different operating systems
.DS_Store
Thumbs.db
*.swp
*.swo

# --- VIRTUAL ENVIRONMENTS ---
# Never push local environments; dependencies are managed via requirements.txt
venv/
env/
.venv/
bin/
lib/
include/

# --- PYTHON & CACHING ---
__pycache__/
*.py[cod]
*$py.class
.pytest_cache/
.coverage
htmlcov/
*.log

# --- SENSITIVE CREDENTIALS (CRITICAL) ---
# Prevents accidental leaking of OpenAI/Gemini API keys
.env
.env.shared
.streamlit/secrets.toml

# --- INSTITUTIONAL DATA (Sprints 5, 7, 10) ---
# Protects PII (Personally Identifiable Information) and financial records
# We ignore actual data but keep the directory structure
data/*.csv
data/*.xlsx
data/*.json
data/*.xml
!data/.gitkeep

# --- AI & ANALYTICS ARTIFACTS ---
.ipynb_checkpoints
models/*.pkl
models/*.joblib
.huggingface/

# --- IDE CONFIGURATIONS ---
# Keeps the repo clean for other developers using different IDEs
.vscode/
.idea/
*.sublime-project
*.sublime-workspace

# --- TEMPORARY & OUTPUTS ---
output/
temp/
docs/_build/
